!!!
%html
  %head
    %meta{ charset: 'utf-8' }
    %meta{ http_equiv: 'X-UA-Compatible', content: 'IE=edge' }
    %meta{ name: 'viewport', content: 'width=device-width, initial-scale=1' }
    %title Changer de mot de passe
    %link{ rel: 'stylesheet', href: '/css/bootstrap.min.css' }
    %style
      html, footer { background-color: whitesmoke; }
      footer { padding: 20px 0 40px 0; border-top: 1px solid #ccc; color: #888; }
      body { background-color: #fff; }
    %script{ src: '/js/jquery.min.js' }
    %script{ src: '/js/bootstrap.min.js' }
  %body
    .container
      .page-header
        %h1 Changer de mot de passe
      - if @notice
        .alert.alert-success
          %h4 Mot de passe changé avec succès
          = @notice
      - if @alert
        .alert.alert-danger
          %h4 Votre mot de passe n'a pas été changé
          = @alert
    .container
      .row
        .col-md-6
          != $doc
        .col-md-6
          .panel.panel-primary
            .panel-heading
              %h4.panel-title Changer de mot de passe
            .panel-body
              %form{ method: :post, url: url('/') }
                .form-group
                  %label.control-label{ for: 'uid' }
                    Nom d'utilisateur :
                  %input#uid{ name: 'uid', class: 'form-control', value: params['uid'] }
                  .help-block
                    Généralement votre prénom, en minuscule, sans espace et sans accent.
                .form-group
                  %label.control-label{ for: 'current_password' }
                    Mot de passe actuel :
                  %input#current_password{ name: 'current_password', type: 'password', class: 'form-control' }
                .form-group
                  %label.control-label{ for: 'password' }
                    Nouveau mot de passe :
                  %input#password{ name: 'password', type: 'password', class: 'form-control' }
                .form-group
                  %label.control-label{ for: 'password_confirmation' }
                    Confirmation du nouveau mot de passe :
                  %input#password_confirmation{ name: 'password_confirmation', type: 'password', class: 'form-control' }
                .text-center
                  %button#submit{ type: 'submit', class: 'btn btn-primary', data: { loading_text: 'Veuillez patienter…' } } Changer de mot de passe
    %footer
      .container
        Copyright © 2014 Romain Tartière
